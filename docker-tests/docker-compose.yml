services:
  mck3dprintfarm:
    # Use locally built image with 0.0.3a tag
    image: mck3dprintfarm:0.0.3a
    # The ENTRYPOINT in the Dockerfile now handles the full startup sequence
    # command: |
    #   sh -c " ... removed ... "
    ports:
      - "3000:3000"
    volumes:
      # Map host uploads to container uploads
      - ../uploads:/app/uploads
      # Map host database directory to /app/data in container
      - ./database:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NEXTAUTH_URL=http://localhost:3000
      - NEXTAUTH_SECRET=your-super-secret-random-string-here-please-change-this
      # Point DATABASE_URL to the new location
      - DATABASE_URL=file:/app/data/dev.db
      # Default admin user credentials (used by create-admin-user.js)
      - DEFAULT_ADMIN_EMAIL=admin@example.com
      - DEFAULT_ADMIN_PASSWORD=Admin123!
      - DEFAULT_ADMIN_NAME=Administrator
      # Default Site Settings (will be used if not set in DB)
      - DEFAULT_PRINT_FARM_TITLE=My Awesome Farm
      - DEFAULT_ORG_NAME=My Organization
      - DEFAULT_ORG_WEBSITE=https://example.com
    networks:
      - printfarm_network

# Removed named volume definition

networks:
  printfarm_network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.201.0.0/24
          gateway: 192.201.0.1 
